If a task is started, set it to "In progress". If a task is completed, remove it.

# TODO

- Add version pinning option for language server
- Improve error messages when dependencies missing
- Add library path configuration support
- Consider adding ESP-specific snippets (WiFi connection, SPIFFS/LittleFS, deep sleep, web server setup)
- Consider adding advanced pattern snippets (state machine, debouncing, moving average filter, PID controller)
- Consider adding communication protocol snippets (I2C master/slave, SPI transactions, Software Serial)
- Consider adding common sensor pattern snippets (DHT sensor, HC-SR04 ultrasonic, button with debouncing)

# In progress
